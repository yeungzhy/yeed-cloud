<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.yeungzhy</groupId>
        <artifactId>yeed-cloud</artifactId>
        <version>20260501</version>
    </parent>

    <artifactId>yeed-generator</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <!-- 依赖 common，复用 BaseEntity、Result、工具类 -->
        <dependency>
            <groupId>com.yeungzhy</groupId>
            <artifactId>yeed-common-data</artifactId>
            <version>20260501</version>
        </dependency>

        <!-- MyBatis-Plus 生成器核心 -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-generator</artifactId>
        </dependency>
        <!-- 模板引擎（选择其一） -->
        <dependency>
            <groupId>org.apache.velocity</groupId>
            <artifactId>velocity-engine-core</artifactId>
            <version>2.4</version>
        </dependency>
        <!-- 数据库驱动（连哪个库就引哪个） -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
                父 POM pluginManagement, skip=true 其实是 防御性/文档化 配置（不写也不会 repackage）
                明确声明这里不打包
            -->
            <!-- 跳过 spring-boot:repackage，避免打包成可执行 fat jar -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <!--
                maven-install-plugin / maven-deploy-plugin 是默认绑定到生命周期的
                所以 skip=true 是必需的, 不写就真会把产物装进本地仓库
            -->
            <!-- 跳过 install，不安装到本地仓库，防止被其他模块误依赖 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-install-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <!-- 跳过 deploy，不发布到远程仓库 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
