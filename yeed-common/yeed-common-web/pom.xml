<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.yeungzhy</groupId>
        <artifactId>yeed-common</artifactId>
        <version>20260501</version>
    </parent>

    <artifactId>yeed-common-web</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.yeungzhy</groupId>
            <artifactId>yeed-common-core</artifactId>
            <version>20260501</version>
        </dependency>

        <!--
            Spring Boot Web（Servlet 栈）：包含 spring-webmvc、spring-web、tomcat、jackson-databind 等。
            web 模块定位为 Servlet/MVC 栈，业务模块（admin/auth）引入 web 模块即拥有完整的 Web 能力，
            无需再单独引入 spring-boot-starter-web。
            ❌ reactive 模块（gateway）禁引本模块。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Bean Validation：Spring Boot 3.x 起 spring-boot-starter-web 不再包含，需显式引入 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Apache HttpClient 5：RestClient 与 OpenFeign 共用的底层高性能 HTTP 客户端（连接池/超时/重试） -->
        <dependency>
            <groupId>org.apache.httpcomponents.client5</groupId>
            <artifactId>httpclient5</artifactId>
        </dependency>

        <!-- FastJson -->
        <dependency>
            <groupId>com.alibaba.fastjson2</groupId>
            <artifactId>fastjson2</artifactId>
        </dependency>

        <!-- MapStruct：对象字段映射 -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>

        <!--
            spring-tx：ExternalApiExceptionHandler 编译期需要访问 org.springframework.dao.DataAccessException
            及其子类（DuplicateKeyException / DataIntegrityViolationException）。
            provided 范围：运行期由 data 模块（mybatis-plus-spring-boot3-starter 传递 spring-tx）提供；
            若业务模块仅引入 web 不引入 data，这些 @ExceptionHandler 不会被匹配，不影响运行。
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
            <scope>provided</scope>
        </dependency>

    </dependencies>


</project>
